# converts objects with id, name and description to only those objects
# from company.models import Service
#
#
# def service_to_card(service: Service):
#     return {'id': service.id, 'name': service.name, 'description': service.description, 'absolute_url': service.get_absolute_url()}